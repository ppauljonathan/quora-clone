<div class="question-show-title"><%= question.try(:title) %></div>

<div class="question-user-details">
  <%= render 'shared/user_profile_picture', user: question.user %>
  <%= render 'shared/user_name', user: question.user %>
</div>

<div class="topics-list", style="padding-left: 5%">
  <div class="question-sub-headings">
    topics:
  </div>
  <% question.try(:topics)&.each do |topic| %>
    <div class="topic"><%= topic.name %></div>
  <% end %>
</div>
